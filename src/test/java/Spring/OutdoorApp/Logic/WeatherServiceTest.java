package Spring.OutdoorApp.Logic;

class WeatherServiceTest {

  /*  @Test
    void getActivitiesAvailable_if_any_activity_does_not_meet_requirements_returns_empty() throws IOException {
        //given
        var mockActualWeatherRepository = mock(ActualWeatherRepository.class);
        //and
        LocalDate date = LocalDate.now();
        //and
        var mockActivityRepository = mock(ActivityRepository.class);
        //and
        var weather = new ActualWeather(date, "icon", -20.0, -20.0, -20.0,
                -20.0, -20.0, -20.0, 2);
        //and
        var mockRequirementsService = mock(RequirementsService.class);
        mockActualWeatherRepository.save(weather);
        //sat
        var toTest = new WeatherService(mockActualWeatherRepository, mockActivityRepository);
        //when
        var result = toTest.getActivitiesAvailable();
        //then
        assertThat(result).isEmpty();
    }*/

}